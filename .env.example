# Database (replace with your PostgreSQL URL)
DATABASE_URL="postgresql://user:password@localhost:5432/monivia_banque?schema=public"

# CSRF / NextAuth secret (generate a random string: openssl rand -base64 32)
CSRF_SECRET="CHANGE_ME_TO_A_RANDOM_SECRET"
